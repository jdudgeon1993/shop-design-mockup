{
  "name": "design-lab-gatekeeper",
  "version": "2.0.0",
  "private": true,
  "description": "Serves this repo's static site, but gated: every path requires a real, server-verified Supabase session belonging to an employee, or it gets coming-soon/index.html instead. Meant to run on Railway in front of the real shopdesignlab.com domain while the site isn't public yet — see the deploy notes shared alongside this file.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node static-server.js"
  }
}
